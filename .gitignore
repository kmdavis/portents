node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.log
coverage/
.tmp/
scratch/
manual/dist/

# pi's own packages, symlinked in for the extension's typecheck (see
# packages/pi/scripts/link-pi.mjs). Outside node_modules so pnpm does not try to
# manage read-only files it cannot chmod.
packages/pi/node_modules/
