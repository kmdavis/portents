{
  "name": "@portent/content",
  "version": "0.0.0",
  "description": "Everything most tables need: the generic content pack plus the packs for systems in common use. Install this unless you want to pick packs by hand.",
  "keywords": [
    "rpg",
    "ttrpg",
    "solo",
    "content",
    "tables",
    "decks"
  ],
  "license": "CC0-1.0",
  "author": "Kevan Davis",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kmdavis/portent.git",
    "directory": "packages/content"
  },
  "homepage": "https://github.com/kmdavis/portent/tree/main/packages/content#readme",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "node --conditions=development --test 'src/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@portent/core": "workspace:*"
  },
  "dependencies": {
    "@portent/content-generic": "workspace:*"
  },
  "devDependencies": {
    "@portent/core": "workspace:*"
  }
}
